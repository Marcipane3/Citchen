---
phase: "01-shared-shopping-list"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - v2/src/features/shopping/listMerge.js
  - v2/src/data/listSync.js
  - v2/src/data/drive.js
  - v2/src/features/shopping/shopping.js
  - v2/sw.js
  - v2/src/version.js
  - v2/src/i18n.js
  - v2/tests/test-list-merge.js
  - v2/tests/run.js
autonomous: true
requirements:
  - REQ-I2
  - REQ-I3A
must_haves:
  truths:
    - "Shopping list survives a full page reload — items are loaded from Drive on start"
    - "Adding an item on device A and refreshing on device B (same Google account) shows the new item"
    - "Two concurrent adds (one per device) both survive — no item is silently dropped"
    - "Going offline queues changes (dirty flag); they push when back online"
    - "mergeList(local, remote) is a pure function with at least 6 unit-test cases passing"
    - "The recipe file rezepte.json is never touched by any list operation"
  artifacts:
    - path: "v2/src/features/shopping/listMerge.js"
      provides: "Pure mergeList(local, remote) export — union + last-writer-per-item by updated ISO string"
      exports: ["mergeList"]
    - path: "v2/src/data/listSync.js"
      provides: "syncListWithDrive(), saveList(items), onStatus(fn), getStatus() exports"
      exports: ["syncListWithDrive", "saveList", "onStatus", "getStatus"]
    - path: "v2/tests/test-list-merge.js"
      provides: "Unit tests for mergeList — minimum 6 cases covering REQ-I2 and REQ-I3A"
      contains: "import { mergeList }"
  key_links:
    - from: "v2/src/features/shopping/shopping.js"
      to: "v2/src/data/listSync.js"
      via: "import * as listSync"
      pattern: "listSync\\.syncListWithDrive"
    - from: "v2/src/data/listSync.js"
      to: "v2/src/data/drive.js"
      via: "drive.createFile(content, LIST_FILE_NAME)"
      pattern: "createFile.*einkaufsliste"
    - from: "v2/src/data/listSync.js"
      to: "v2/src/features/shopping/listMerge.js"
      via: "import { mergeList }"
      pattern: "mergeList\\(localItems"
    - from: "v2/sw.js"
      to: "v2/src/data/listSync.js"
      via: "SHELL array entry"
      pattern: "listSync\\.js"
---

<objective>
Implement item-level Drive persistence for the shopping list: new pure mergeList() function,
new listSync.js module mirroring sync.js, minimal drive.js extension, and wire shopping.js
to call syncListWithDrive() on load and on save. Add a manual Refresh button. Update SW shell
and version stamp.

Purpose: Delivers REQ-I2 (two-way shared list via shared Drive file with item-level merge)
and REQ-I3A (multi-party-ready item schema with author field, union-merge for 2+ authors).
The recipe sync path is left completely untouched.

Output: einkaufsliste.json on Drive; listSync.js + listMerge.js modules; 6+ unit tests;
drive.js extended with fileName param; shopping.js wired for Drive sync; SW SHELL updated.
</objective>

<execution_context>
@C:\Users\malbr\.claude\gsd-core\workflows\execute-plan.md
@C:\Users\malbr\.claude\gsd-core\templates\summary.md
</execution_context>

<context>
@C:\Claude_files\Koch_Project\.planning\PROJECT.md
@C:\Claude_files\Koch_Project\.planning\ROADMAP.md
@C:\Claude_files\Koch_Project\.planning\STATE.md
@C:\Claude_files\Koch_Project\.planning\phases\01-shared-shopping-list\01-RESEARCH.md
@C:\Claude_files\Koch_Project\.planning\phases\01-shared-shopping-list\01-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create listMerge.js (pure) and test-list-merge.js (6+ cases); register in run.js</name>
  <files>v2/src/features/shopping/listMerge.js, v2/tests/test-list-merge.js, v2/tests/run.js</files>
  <read_first>
    - v2/src/data/decideSync.js — exact file header style, export-only pattern, no imports
    - v2/tests/test-decide-sync.js — import pattern (runner.js + pure function), one test() call per behavior
    - v2/tests/run.js — import list; new import goes before the final runner import, after test-decide-sync.js
  </read_first>
  <behavior>
    - mergeList([], []) returns []
    - mergeList with remote item having later updated ISO string: remote item wins (name from remote)
    - mergeList with local item having later updated ISO string: local item wins
    - mergeList: item with deleted:true in remote propagates — merged result has deleted:true for that id
    - mergeList: two authors adding different ids — both items present in merged result (length === 2)
    - mergeList: idempotent — mergeList(items, items) has same length and same id as input
    - mergeList: concurrent adds from both local and remote each contributing 1 unique id — merged length === 2 (REQ-I3A multi-party case)
  </behavior>
  <action>
    Create v2/src/features/shopping/listMerge.js as a pure ESM module. File header comment explains
    purpose and reuse context, matching the decideSync.js header style (5-line block comment, then export).
    No imports, no I/O, no side effects. Export one function:

      export function mergeList(local = [], remote = []) — builds a Map keyed by item.id; iterates
      local first, then remote: if remote item's updated > existing updated (ISO string comparison),
      remote wins. Returns [...byId.values()]. Returns all items including those with deleted:true;
      callers filter tombstones before rendering.

    Create v2/tests/test-list-merge.js following test-decide-sync.js structure exactly:
      - Import: import { test, assertEqual } from "./runner.js";
      - Import: import { mergeList } from "../src/features/shopping/listMerge.js";
      - Write exactly 7 test() calls, one per behavior above, with German names matching the
        test-decide-sync.js naming pattern (e.g., "mergeList: beide leer → leeres Array").
      - For the deleted tombstone test: local has deleted:false at updated "T1", remote has deleted:true
        at updated "T2". Assertion: merged[0].deleted === true.
      - For the two-author test: local has id "li-1" author "marcel" updated "T1", remote has
        id "li-2" author "partner" updated "T1". Assertion: merged.length === 2.
      - For idempotency: items array with one entry; mergeList(items, items).length === 1 and
        mergeList(items, items)[0].id === items[0].id.

    Edit v2/tests/run.js: add the line  import "./test-list-merge.js";  after the existing
    import "./test-decide-sync.js"; line (line 15), before the import { run } line.

    Do NOT put fenced code blocks in this action. Identify the function signature as
    mergeList(local = [], remote = []) and the Map pattern by id field.
  </action>
  <verify>
    <automated>node v2/tests/run.js</automated>
  </verify>
  <acceptance_criteria>
    - v2/src/features/shopping/listMerge.js exists and has no imports
    - v2/tests/test-list-merge.js exists with exactly 7 test() calls
    - node v2/tests/run.js exits 0 with count >= 173 tests (166 baseline + 7 new)
    - All 7 test cases pass — zero failures
    - run.js has import "./test-list-merge.js"; after test-decide-sync.js import
  </acceptance_criteria>
  <done>mergeList pure function exists, all 7 unit tests pass, baseline 166 tests still pass</done>
</task>

<task type="auto">
  <name>Task 2: Extend drive.js (fileName param on createFile); create listSync.js</name>
  <files>v2/src/data/drive.js, v2/src/data/listSync.js</files>
  <read_first>
    - v2/src/data/drive.js — createFile() at line 159 (current signature); FILE_NAME constant at line 10;
      driveFetch(), readFile(), updateFile() signatures; isSignedIn() at line 23
    - v2/src/data/sync.js — full file for the exact module shape to mirror:
      imports block (lines 9–13), META_KEY const, status listener pattern (lines 40–45),
      syncWithDrive() structure (lines 75–143), saveCollection() pattern (lines 149–174)
    - v2/src/data/decideSync.js — decideSync() signature
    - v2/src/data/db.js — kvGet(key, default), kvSet(key, value), put(store, obj), get(store, id) signatures
    - v2/.planning/phases/01-shared-shopping-list/01-PATTERNS.md — full listSync.js code excerpts
      (loadLocalList helper, syncListWithDrive full body, saveList full body)
  </read_first>
  <action>
    CHANGE 1 — drive.js createFile() at line 159:
    Change the function signature from  createFile(contentString)  to
    createFile(contentString, fileName = FILE_NAME)  and change the meta object's name property
    from  name: FILE_NAME  to  name: fileName . This is a 2-line change; the rest of the
    function body is untouched. All existing callers of createFile(content) with one argument
    still work because of the default.

    CREATE — v2/src/data/listSync.js:
    Mirror the shape of sync.js exactly. Imports:
      import * as db from "./db.js";
      import * as drive from "./drive.js";
      import { decideSync } from "./decideSync.js";
      import { mergeList } from "../features/shopping/listMerge.js";

    Constants:
      const META_KEY = "listMeta";
      const LIST_FILE_NAME = "einkaufsliste.json";
      const LIST_DB_ID = "current";

    Status listener block (copy sync.js lines 40–45 exactly, same names):
      let statusListeners = new Set();
      let status = "";
      export function onStatus(fn) { ... }
      function setStatus(msg) { ... }
      export function getStatus() { return status; }

    Private helper loadLocalList():
      Reads db.get("lists", LIST_DB_ID). Returns row.items or []. One-time migration:
      for items without an id field, assign id = "li-" + (Date.now() + index),
      updated = new Date().toISOString(), author = "local", deleted = false.
      If any item was migrated, write back to db.put("lists", ...) with new updated timestamp.
      Returns the (possibly migrated) items array.

    export async function syncListWithDrive():
      Guard: if (!drive.isSignedIn()) return { changed: false }.
      setStatus("Verbinde mit Drive…").
      let meta = await db.kvGet(META_KEY, {}).
      let fileId = meta.fileId.
      Wrap in try/catch; catch sets setStatus(e.status === 401 ? "Anmeldung abgelaufen" :
      "Offline — lokale Daten") and returns { changed: false, error: e }.

      if (!fileId) branch: create file. loadLocalList() → JSON.stringify({version:1, updated, items})
      → drive.createFile(content, LIST_FILE_NAME) → store meta with fileId, dirty:false,
      updated, lastSync, source:"drive" → return { changed: false, meta }.

      const remote = await drive.readFile(fileId).
      decideSync({hasRemote:true, localUpdated: meta.updated||"", remoteUpdated: remote.updated||"",
        dirty: meta.dirty||false, source: meta.source||"local"}).

      "push" branch: loadLocalList() → updateFile(fileId, ...) → update meta (dirty:false, updated,
      lastSync, source:"drive") → return { changed: false, meta }.

      "pull" or "conflict" branch (list merge — unlike recipes, conflict IS resolved):
        loadLocalList() → mergeList(localItems, remote.items||[]) → merged array.
        db.put("lists", {id: LIST_DB_ID, items: merged, updated: remote.updated||new Date().toISOString()}).
        update meta (updated: remote.updated, dirty:false, lastSync, source:"drive").
        Push the merged result back to Drive: drive.updateFile(fileId, JSON.stringify({version:1,
          updated: new Date().toISOString(), items: merged})).
        return { changed: true, items: merged, meta }.

      "noop" branch: update meta.lastSync only → return { changed: false, meta }.
      All branches: setStatus("Synchronisiert ✓") before returning.

    export async function saveList(items):
      Mirror sync.js saveCollection() exactly:
        updated = new Date().toISOString().
        db.put("lists", {id: LIST_DB_ID, items, updated}).
        read meta, set dirty:true, write meta back.
        If drive.isSignedIn() && navigator.onLine !== false: try to updateFile(meta.fileId, ...)
          set dirty:false, lastSync, source:"drive" on success.
          On catch: console.warn("Drive-Push fehlgeschlagen (bleibt dirty):", e).
        Return meta.

    The key invariant: drive.createFile() is only called with LIST_FILE_NAME as the second
    argument, never without an argument, ensuring rezepte.json is never touched by list code.
  </action>
  <verify>
    <automated>node v2/tests/run.js</automated>
  </verify>
  <acceptance_criteria>
    - v2/src/data/drive.js createFile() signature is now createFile(contentString, fileName = FILE_NAME)
    - v2/src/data/listSync.js exists and exports syncListWithDrive, saveList, onStatus, getStatus
    - drive.js still passes all 166 baseline tests (node v2/tests/run.js exits 0, count >= 173)
    - listSync.js imports mergeList from "../features/shopping/listMerge.js" (grep confirms)
    - listSync.js uses LIST_FILE_NAME ("einkaufsliste.json") as the fileName arg to createFile — grep
      for "einkaufsliste.json" in listSync.js returns 1+ matches
    - listSync.js never references "rezepte.json" (grep returns 0 matches in listSync.js)
  </acceptance_criteria>
  <done>
    drive.js createFile accepts fileName param with default; listSync.js mirrors sync.js shape
    and produces einkaufsliste.json on Drive; no existing tests broken
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire shopping.js (Drive sync + Refresh button + tombstone filter); update sw.js, version.js, i18n.js</name>
  <files>
    v2/src/features/shopping/shopping.js,
    v2/sw.js,
    v2/src/version.js,
    v2/src/i18n.js
  </files>
  <read_first>
    - v2/src/features/shopping/shopping.js — full file; note:
        imports block (lines 1–10), LIST_ID/ITEMS constants (lines 12–19),
        load() function (lines 21–25), save() function (lines 26–28),
        shopAdd() function (lines 50–58) where ITEMS.push happens at line 56,
        renderShopping() extra: block (lines 69–103) where buttons are injected,
        paintList() at line 105 — note doneCount at line 108,
        DOM event wiring for existing .sl-share button (lines 197–198) as the
        pattern for adding new button wiring after load().then()
    - v2/sw.js — CACHE constant at line 9; SHELL array, shopping entries at lines 43–44
    - v2/src/version.js — BUILD and APP_VERSION at lines 2–3; CHANGELOG array starts line 6
    - v2/src/i18n.js — shopping: blocks at lines 186–196 (de), 395–405 (en), 604–614 (es), 813–823 (da)
    - v2/.planning/phases/01-shared-shopping-list/01-PATTERNS.md — shopping.js diff excerpts
      (item creation patch, save() patch, load() patch, render extra: patch, button wiring,
      tombstone filter in paintList)
  </read_first>
  <action>
    SHOPPING.JS changes (6 targeted edits):

    Edit 1 — Add import at top (after existing imports, before const LIST_ID):
      import * as listSync from "../../data/listSync.js";
      import * as drive from "../../data/drive.js";

    Edit 2 — shopAdd() at line 56, the ITEMS.push call. Replace the existing push object
    (which has name, cat, icon, qty, done, amount, unit) with a new object that adds:
      id: "li-" + Date.now(),
      updated: new Date().toISOString(),
      author: "local",
      deleted: false
    Keep all existing fields (name, cat, icon, qty:1, done:false, amount:null, unit:null).
    Also add  it.updated = new Date().toISOString();  after  it.qty++;  in the existing-item
    branch (where qty is incremented for a duplicate).

    Edit 3 — save() at lines 26–28. After the existing db.put(...).catch(()=>{}) line, add:
      listSync.saveList(ITEMS).catch(() => {});
    This wires dirty-flag offline queue on every mutation.

    Edit 4 — load() at lines 21–25. After  sortMode = await db.kvGet(SORT_KEY, "aisle");
    add a non-blocking Drive sync call:
      listSync.syncListWithDrive().then((result) => {
        if (result.changed && Array.isArray(result.items)) {
          ITEMS = result.items.filter(x => !x.deleted);
          paintList(container); paintCatalog(container);
        }
      }).catch(() => {});
    Note: load() does not have access to container. The container variable is in the outer
    scope of renderShopping. Move the sync call into the load().then() in renderShopping at
    line 102 instead: after  load().then(() => { paintList(container); paintCatalog(container);
    add the listSync.syncListWithDrive() chain INSIDE that .then() callback.

    Edit 5 — renderShopping() extra: string. Add two new buttons after the existing
    .shop-add div and before the closing backtick of the extra: value:
      <button class="sl-refresh" aria-label="${t('shopping.refresh')}">${t('shopping.refreshBtn')}</button>
      <button class="sl-link-partner">${t('shopping.linkPartner')}</button>
    (These will be wired in the next edit.)

    Edit 6 — After the existing shareBtn wiring (line 198), add Refresh button wiring:
      const refreshBtn = container.querySelector(".sl-refresh");
      if (refreshBtn) refreshBtn.onclick = () => {
        refreshBtn.disabled = true;
        listSync.syncListWithDrive().then((result) => {
          if (result.changed && Array.isArray(result.items)) {
            ITEMS = result.items.filter(x => !x.deleted);
            paintList(container); paintCatalog(container);
          }
          refreshBtn.disabled = false;
        }).catch(() => { refreshBtn.disabled = false; });
      };

    paintList() tombstone filter: at line 108, before  const doneCount = ITEMS.filter(...)
    add:  const visible = ITEMS.filter(x => !x.deleted);  and then replace the two ITEMS
    references in the doneCount and openCount lines with visible. Keep the rest of paintList
    operating on ITEMS (the full in-memory array including tombstones); only the count/sub
    display and the html rendering use visible. Do NOT add a second separate visible filter
    below — the render loop at line 147  indexed = ITEMS.map(...)  should also be changed to
    use visible so tombstoned items do not appear in the DOM.

    SW.JS changes:
    Line 9: change  const CACHE = "koch-v2.9-1";  to  const CACHE = "koch-v2.10-1";
    After line 44 (the "./src/features/shopping/shopping.js" entry), add two new SHELL entries:
      "./src/features/shopping/listMerge.js",
      "./src/data/listSync.js",

    VERSION.JS changes:
    Line 2: change BUILD to  "2026-06-17-v2.10"
    Line 3: change APP_VERSION to  "2.10"
    Line 6 (after the CHANGELOG opening bracket), insert new entry first:
      { v: "v2.10", txt: "Einkaufsliste mit Partner teilen: Verbinde deine Liste über Google Drive — beide sehen dieselben Artikel und können gleichzeitig hinzufügen. Aktualisieren-Knopf holt den neuesten Stand." },

    I18N.JS changes — add keys to the shopping: block in each of the 4 language sections.
    The closing  }  of each shopping: block is the insertion point (add before it).
    File rule: use curly/typographic quotes inside string values, never straight ".

    DE (line 196, before closing brace of shopping: block):
      refreshBtn: "🔄 Aktualisieren", refresh: "Einkaufsliste aktualisieren",
      linkPartner: "Partner verknüpfen", unlinkPartner: "Partner trennen",
      syncStatus: "Synchronisiert ✓", syncPending: "Sync ausstehend",

    EN (line 405, before closing brace of shopping: block):
      refreshBtn: "🔄 Refresh", refresh: "Refresh shopping list",
      linkPartner: "Link partner", unlinkPartner: "Unlink partner",
      syncStatus: "Synced ✓", syncPending: "Sync pending",

    ES (line 614, before closing brace of shopping: block):
      refreshBtn: "🔄 Actualizar", refresh: "Actualizar lista de compra",
      linkPartner: "Vincular pareja", unlinkPartner: "Desvincular pareja",
      syncStatus: "Sincronizado ✓", syncPending: "Sincronización pendiente",

    DA (line 823, before closing brace of shopping: block):
      refreshBtn: "🔄 Opdater", refresh: "Opdater indkøbsliste",
      linkPartner: "Tilknyt partner", unlinkPartner: "Fjern partner",
      syncStatus: "Synkroniseret ✓", syncPending: "Synkronisering afventer",
  </action>
  <verify>
    <automated>node v2/tests/run.js</automated>
  </verify>
  <acceptance_criteria>
    - node v2/tests/run.js exits 0 with all 173+ tests passing (SW shell guard will confirm new SHELL entries)
    - sw.js CACHE is "koch-v2.10-1" (grep -c "koch-v2.10-1" v2/sw.js returns 1)
    - sw.js SHELL includes "./src/features/shopping/listMerge.js" and "./src/data/listSync.js"
      (grep returns matches for both)
    - version.js APP_VERSION is "2.10"
    - i18n.js contains "refreshBtn" in all 4 shopping: blocks (grep -c "refreshBtn" v2/src/i18n.js
      returns 4)
    - shopping.js imports listSync (grep -c "listSync" v2/src/features/shopping/shopping.js >= 1)
    - shopping.js ITEMS.push in shopAdd() includes an "id:" field (grep -c '"id":' on that line,
      or grep for "li-.*Date.now" in shopping.js)
    - shopping.js paintList uses visible.filter before render (grep -c "x.deleted" v2/src/features/shopping/shopping.js >= 1)
    - shopping.js has a .sl-refresh button wired via onclick (grep -c "sl-refresh" shopping.js >= 2)
    - No straight double-quotes inside i18n string values for the new keys (manual scan of added lines)
  </acceptance_criteria>
  <done>
    Shopping list persists to Drive on save, syncs on load, re-paints if remote changed,
    has a working Refresh button, tombstones are filtered from render; SW cache busted to v2.10;
    all 173+ tests green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Drive API → listSync.js | Remote JSON content is untrusted; malformed or attacker-controlled items arrive here |
| shopping.js DOM render | Merged item fields (name, cat) from Drive must be escaped before innerHTML injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Drive content → DOM (shopping.js paintList) | mitigate | esc(itemLabel(it)) already wraps all item.name/cat in rowHTML; merged remote items flow through the same rowHTML path — no separate render branch allowed |
| T-01-02 | Tampering | Concurrent Drive writes (two devices push simultaneously) | mitigate | mergeList() + decideSync() — never blind-overwrite; conflict branch calls mergeList then pushes merged result |
| T-01-03 | Elevation of Privilege | Stale/expired OAuth token during sync | mitigate | drive.js driveFetch() already clears TOKEN and throws DriveError(status:401) on 401 response; listSync.js catch block checks e.status === 401 and surfaces "Anmeldung abgelaufen" |
| T-01-04 | Tampering | listSync accidentally targets rezepte.json | mitigate | drive.createFile() called with LIST_FILE_NAME constant ("einkaufsliste.json") as second arg; acceptance criteria grep verifies no "rezepte.json" reference in listSync.js |
| T-01-05 | Denial of Service | Drive sync race on page load overwrites user's in-progress edits | mitigate | syncListWithDrive() returns {changed, items}; shopping.js only updates ITEMS and repaints if changed===true; user's typed-but-not-saved item is not in ITEMS yet (add happens on button click) |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this phase — all code is in-repo ESM modules; CDN APIs (gapi) already present in index.html |
</threat_model>

<verification>
Full phase verification:
- node v2/tests/run.js — must exit 0 with 173+ tests passed, 0 failed
- grep -c "einkaufsliste.json" v2/src/data/listSync.js — must return >= 1
- grep -rn "rezepte.json" v2/src/data/listSync.js — must return 0 (no matches)
- grep -c "koch-v2.10-1" v2/sw.js — must return 1
- grep -c "listSync.js" v2/sw.js — must return 1 (SHELL entry present)
- grep -c "listMerge.js" v2/sw.js — must return 1 (SHELL entry present)
- grep -c "refreshBtn" v2/src/i18n.js — must return 4 (one per language)
- grep -n "fileName = FILE_NAME" v2/src/data/drive.js — must return 1 match
</verification>

<success_criteria>
- mergeList(local, remote) is pure, exported from listMerge.js, passes 7 unit tests
- syncListWithDrive() creates einkaufsliste.json on first run; reads/merges/writes on subsequent runs
- saveList(items) writes IndexedDB immediately and attempts Drive push; sets dirty:true on failure
- shopping.js calls syncListWithDrive() on page load; re-paints if changed===true
- Refresh button triggers manual sync; button is disabled during in-flight request
- Tombstone items (deleted:true) never reach the DOM
- rezepte.json is never touched by any list module (grep gate passes)
- All 173+ tests green; SW CACHE "koch-v2.10-1"; APP_VERSION "2.10"
</success_criteria>

## Artifacts this phase produces

| File | Status | Purpose |
|------|--------|---------|
| v2/src/features/shopping/listMerge.js | NEW | Pure mergeList() function |
| v2/src/data/listSync.js | NEW | List sync lifecycle (mirrors sync.js) |
| v2/tests/test-list-merge.js | NEW | 7 unit tests for mergeList |
| v2/src/data/drive.js | MODIFIED | createFile() accepts optional fileName param |
| v2/src/features/shopping/shopping.js | MODIFIED | Drive wiring, id/updated/author/deleted on items, Refresh button, tombstone filter |
| v2/sw.js | MODIFIED | SHELL adds listMerge.js + listSync.js; CACHE bumped to "koch-v2.10-1" |
| v2/src/version.js | MODIFIED | BUILD + APP_VERSION = "2.10"; CHANGELOG entry added |
| v2/src/i18n.js | MODIFIED | 6 keys added to shopping: block in all 4 languages |
| v2/tests/run.js | MODIFIED | import "./test-list-merge.js"; registered |

<output>
Create .planning/phases/01-shared-shopping-list/01-01-SUMMARY.md when done.
</output>
